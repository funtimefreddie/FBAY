<!DOCTYPE html>
<html>
<head>
  <title>Fbay</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

<% if user_signed_in? %> 

  Logged in as <strong><%= current_user.name %></strong><br>

  <%= link_to 'Edit profile', edit_user_path(current_user) %></td>
  <%= link_to 'My items', my_items_path %>
  <%= link_to 'My bidded items', bidded_items_path %>
  <%= link_to 'Search items', items_path %>
  <%= link_to 'New Item', new_item_path %>
  <%= link_to "Logout", destroy_user_session_path, method: :delete %> 

<% else %> 

  <%= link_to 'Search items', items_path %>
  <%= link_to 'Login / register', new_user_session_path %></td>

<% end %> 

<%= yield %>

</body>
</html>
